[project]
name = "mcp-trajectory-judge"
version = "0.1.0"
description = "Replays MCP tool-call trajectories against a deterministic sandbox and judges them."
requires-python = ">=3.10"
license = { text = "Evaluation-Only Licence 1.0 (source-available, not open source); see LICENSE" }
authors = [{ name = "Vo Ba Hoang Minh" }]
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
